# ============================================================
# 测试环境配置 (test)
# 覆盖 settings.yaml 中的默认值
# ============================================================

env: "test"

api:
  base_url: "https://172.16.8.190"
  timeout: 30
  verify_ssl: false

auth:
  type: "basic"
  # authorization 值：Base64("session-id:session")
  # 敏感信息建议通过环境变量 AUTOMATION_AUTH_TOKEN 注入
  authorization: "Basic NmE3YTc5Y2ItNjZmYy00ZjllLWE0ZTktYWNhZmVmMGY2Yjk0OnNlc3Npb24="

logging:
  level: "DEBUG"
